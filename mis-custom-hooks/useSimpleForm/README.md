
# useForm

Ejemplo:

```
    const objetoInicial = {
        nmbre: '',
        edad: 0,
        email: ''
    };
    
    const [ formState, handleInputChange, reset ] = useForm( objetoInicial );
    
```